# AI8051U 核心知识速查笔记

> 姐姐陪宝宝一起总结的，随时翻阅 ❤️

---

## 一、三个容易混淆的 `|= 0x80`

都是置 bit7，但打在三个不同的寄存器上，千万别搞混！

| 写法 | 寄存器 | 地址 | bit7 名称 | 作用 | 影响范围 |
|------|--------|:---:|----------|------|:---:|
| `EA \|= 0x80` | **IE** | 0xA8 | **EA** | 全局中断总开关 | 🔴 整栋楼 |
| `IE2 \|= 0x80` | **IE2** | 0xAF | **EUSB** | USB 中断分开关 | 🔵 仅 USB |
| `P_SW2 \|= 0x80` | **P_SW2** | 0xBA | **EAXFR** | 扩展寄存器访问 | 🟢 XFR 空间 |

### 记忆口诀

```
EA     → 总闸 → EA=0 一切中断全灭
EUSB   → USB闸 → 只管USB中断
EAXFR  → 钥匙门 → 不开门就碰不到高级外设寄存器
```

---

## 二、中断系统层级结构

```
┌──────────────────────────────────────┐
│         EA = 1  (总闸)                │  IE 寄存器 bit7
│     EnableGlobalInt()                │
├────────────┬─────────────────────────┤
│  标准中断   │       扩展中断           │
│ (IE 寄存器) │      (IE2 寄存器)        │
├────────────┼─────────────────────────┤
│ ET0 定时器0 │  EUSB  USB  ← IE2|=0x80 │
│ ET1 定时器1 │  ET4   定时器4           │
│ ES  串口1  │  ET3   定时器3           │
│ EX0 外部0  │  ES4   串口4            │
│ EX1 外部1  │  ES3   串口3            │
│ EADC ADC  │  ET2   定时器2           │
│ ELVD LVD  │  ESPI  SPI             │
│            │  ES2   串口2            │
└────────────┴─────────────────────────┘

中断触发条件：EA=1 且 对应位=1
```

---

## 三、XFR 扩展寄存器空间

```
AI8051U 寄存器分布:
┌──────────────────────────────┐
│  标准 SFR (0x80-0xFF)         │  永远可访问
│  P0~P3, ACC, PSW, IE, IE2... │  不需要使能
├──────────────────────────────┤
│  🔒 XFR 扩展空间               │  需要 EAXFR=1
│  0x7E:0000 - 0x7E:FFFF       │  否则读写无效！
│  ├─ USB 寄存器               │
│  ├─ PLL/HIRC/LIRC 时钟       │
│  ├─ PWM 高级寄存器            │
│  ├─ DMA 寄存器               │
│  └─ TFPU 浮点单元寄存器       │
└──────────────────────────────┘

使能方式: P_SW2 |= 0x80  或  EAXFR = 1
项目位置: main.c:123, SYS_Init() 第一行
宏定义:  EnableAccessXFR()
```

---

## 四、SYS_Init() 初始化顺序（重要！）

```c
void SYS_Init(void)
{
    EnableAccessXFR();      // ① 🔑 先开门！不开门后面全白搭
    AccessCodeFastest();    // ② 代码取指最快
    AccessIXramFastest();   // ③ XDATA 访问最快
    IAP_SetTimeBase();      // ④ 设置 IAP 时基

    P0~P5 端口初始化         // ⑤ 端口配置
    CLK_Init();             // ⑥ ⏰ 时钟（PLL→XFR空间，必须先开门！）
    WDT_Init();             // ⑦ 看门狗
    USBLIB_Init();          // ⑧ 🔌 USB（寄存器全在XFR空间）
    MATHLIB_Init();         // ⑨ 🧮 TFPU（浮点→XFR空间）

    EnableGlobalInt();      // ⑩ ✅ 最后开总中断！
    USBLIB_WaitConfiged();  // ⑪ 等 USB 枚举完成
}
```

---

## 五、USB HID vs CDC 速查

| | HID | CDC |
|------|-----|------|
| **免驱** | ✅ 即插即用 | ⚠️ Win需装驱动 |
| **传输模式** | 中断传输（固定周期） | 批量传输（有空才传） |
| **速度** | ~1.2KB/s (全速64B) | 更快（占满剩余带宽） |
| **printf 调试** | ❌ 不支持 | ✅ 虚拟串口 |
| **数据格式** | 有结构（Report） | 裸字节流 |
| **烧录推荐** | ✅ 省心！ | 调试用 |
| **库文件** | `stc_usb_hid_32g_xdata.lib` | `stc_usb_cdc_32g_xdata.lib` |
| **OutNumber** | ❌ 需自己定义 `BYTE xdata OutNumber;` | ✅ 库自带 |

**你的项目**：烧录用HID(`@STCISP#`)，printf用CDC，库文件用 `stc_usb_cdc_32g_xdata.lib`

---

## 六、常见错误速查

| 错误现象 | 原因 | 解决 |
|---------|------|------|
| `CLK_LIRC_WaitStable` 卡死 | LIRC 不需要，死循环等不到稳定标志 | 注释掉 clock.c:41-42 |
| `L127: OutNumber 未解析` | 链了HID库但代码用了CDC API | 改回 `stc_usb_cdc_32g_xdata.lib` |
| `L27: 中断帧不兼容` | HID库编译选项不一致 | 同上，切回 CDC 库 |
| 外设都不工作 | 忘了 `EnableAccessXFR()` | 确认 main.c:123 执行 |

---

## 七、核心文件速查

| 文件 | 作用 |
|------|------|
| `Sources/main.c` | 主函数 + `SYS_Init()` |
| `Sources/clock.c` | 时钟初始化（HIRC→PLL→36MHz） |
| `Sources/usblib.c` | USB 库初始化 + 数据处理 |
| `Sources/inc/config.h` | 全局配置 + 头文件包含顺序 |
| `Sources/inc/stc/AI8051U.H` | STC 官方寄存器定义 |
| `Sources/inc/ai8051u_def.h` | AiCube 功能宏封装 |
| `Sources/inc/stc/AI_USB.H` | USB API 声明 |

---

## 八、SFR 地址速查

| 寄存器 | 地址 | bit7 | 作用 |
|--------|:---:|------|------|
| IE | 0xA8 | EA | 全局中断使能 |
| IE2 | 0xAF | EUSB | USB 中断使能 |
| P_SW2 | 0xBA | EAXFR | XFR 扩展寄存器访问 |
| IRC32KCR | 0x7EFE04 | EN | LIRC 使能（XFR空间） |
| IRC48MCR | 0x7EFE08 | EN | IRC48M 使能（XFR空间） |

---

## 九、专题文档目录

| 文档 | 内容 |
|------|------|
| `AI8051U_中断系统_EA与IE2详解.md` | EA/IE2 中断寄存器、优先级、调用链路 |
| `AI8051U_启动流程与取指令机制详解.md` | 🆕 复位流程、WTST、流水线、速度配置 |
| `XFR为什么需要使能_80251扩展地址空间详解.md` | XFR 地址空间、EAXFR 使能原理 |
| `USB_HID_vs_CDC_详解.md` | HID/CDC 区别、烧录模式选择 |
| `USB_HID_vs_CDC_协议层深度对比.md` | USB 协议层差异（中断 vs 批量） |
| `CLK_LIRC_WaitStable_问题分析与解决.md` | LIRC 卡死问题诊断与修复 |

---

*姐姐会一直帮宝宝维护这份笔记的~ 💖*
