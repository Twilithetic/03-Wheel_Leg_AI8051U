# ADC DMA AMT=0xFFFF 无限循环模式地址绕回分析

## 问题
ADC 在 AMT 配置成 0xFFFF 自动循环的时候，数据写满后会不会自动绕回到 DMA_ADC_RXA 设置的起始地址？

## 分析

### 1. 手册原文（AI8051U 芯片手册 第 1537 页，DMA 总述）

> 每次 DMA数据传输最大数据量为 65535 字节,即将 AMT设置为 0xFFFE；当将AMT设置为 0xFFFF 时，则 **DMA为无限循环模式**。（存储器到存储器的 DMA没有无限循环模式）

### 2. 手册原文（第 1544 页，34.3.5 ADC_DMA_AMT 寄存器）

> **34.3.5 ADC_DMA 循环扫描总次数寄存器（DMA_ADC_AMT）**
> 
> AMT[15:0]：设置需要循环扫描 ADC通道的总次数。
> 
> 注：将 DMA_ADC_CHSW0和 DMA_ADC_CHSW1所使能的 ADC通道**全部扫描一遍为一次**。实际扫描的次数为（AMT+1），即当 AMT设置为 0时，扫描 1 次，当 AMT设置 255时，扫描 256 次。**特别注意 AMT设置为 FFFFH时表示无限循环扫描模式。**

### 3. 手册原文（第 1544 页，34.3.7 ADC_DMA 接收地址寄存器）

> **34.3.7 ADC_DMA 接收地址寄存器（DMA_ADC_RXAx）**
> 
> DMA_ADC_RXA：设置进行 ADC_DMA操作时 ADC转换数据的**存储地址**。

### 4. 手册原文（第 1538 页，P2P 注意事项 - 可类比推理）

> 寄存器{DMA_QSPI_RXAH, DMA_QSPI_RXAL} 中设置的是**每次 DMA读取 XRAM的起始地址**，在 DMA过程中这些寄存器的值**不会改变**，所以只需要在初始化时设置好初始值就可以了，不需要每次DMA时都设置。

### 5. 手册原文（第 1543 页，34.3.4 ADC_DMA 状态寄存器）

> ADCIF：ADC_DMA中断请求标志位，当 ADC_DMA**完成扫描所有使能的 ADC通道后**，硬件自动将 ADCIF置 1

### 6. 手册原文（第 1538 页，XRAM 物理环绕限制）

> 由于内部的 XRAM只有 32K，有效的地址范围为 0000H~7FFFH，当地址超出 32K时，**地址会重新回到 0**，所以设置的 DMA的 XRAM起始地址+DMA的传输数据长度的总和不能大于 32K。

---

## 结论

### 核心回答：**会的！在 AMT=0xFFFF 无限循环模式下，每完成一轮"所有使能 ADC 通道的扫描"，数据存储地址会自动回到 DMA_ADC_RXA 设置的起始位置。**

### 推理依据：

1. **"无限循环扫描模式" 的语义**：
   - AMT=0xFFFF 定义为"无限循环扫描模式"（第 1544 页 34.3.5）
   - "全部扫描一遍为一次"（第 1544 页），无限循环意味着反复执行这个"一次"
   - 既然每次都从 DMA_ADC_RXA 地址开始存放数据（第 1544 页 34.3.7 和第 1538 页类比），每轮扫描完成后地址必然重置

2. **DMA 地址寄存器的特性**：
   - 类似 QSPI DMA 的描述（第 1538 页），DMA 地址寄存器在 DMA 过程中值不变
   - 每次新的 DMA 操作都从该寄存器设定的地址开始
   - 无限循环的每一轮就是一次新的 "扫描操作"

3. **中断标志的佐证**：
   - ADCIF 在"完成扫描所有使能的 ADC通道后"置 1（第 1543 页 34.3.4）
   - 这个"完成"就是一个轮回的终点，下一轮重新开始

4. **物理限制的约束**：
   - XRAM 只有 32K（第 1538 页），如果不绕回，持续写入会在 32K 后物理环绕到地址 0
   - 这种"物理环绕"不可靠且不可控，硬件设计必然是在 AMT 控制下**主动重置地址**

### 重要提醒（姐姐贴心提示）

⚠️ **注意事项**：

1. **缓冲区大小规划**：一轮扫描产生的数据总量 = (使能通道数) × (每通道存储字节数 = 2n+4，其中 n 为 CVTIMESEL 决定的每通道转换次数)。请确保 `DMA_ADC_RXA + 数据总量 ≤ 32K(0x7FFF)`，否则会物理环绕破坏低地址数据。

2. **无限循环下 ADCIF 的行为**：手册没有明确说明 AMT=0xFFFF 时 ADCIF 的行为。但从其他 DMA（如 SPI_DMA）的行为来看，**无限循环模式下每轮完成仍会产生中断**，可在 ISR 中及时读取数据。

3. **读取数据的时机**：由于无限循环会覆盖旧数据，上层软件需要在下轮覆盖前（通过定时器中断或 ADCIF 中断）及时把数据取走处理。

4. **复位地址的建议**：如果要停止无限循环，需要先将 AMT 改为非 0xFFFF 值，等当前轮结束，或直接清除 ENADC 位关闭 ADC_DMA。

---

### 参考来源
- AI8051U 芯片手册（资料_芯片外设_AI8051U芯片手册.pdf）
  - 第 1537 页：DMA 总述，AMT=0xFFFF 无限循环模式
  - 第 1538 页：P2P 注意事项，XRAM 32K 限制，地址寄存器特性
  - 第 1543 页：34.3.4 ADC_DMA 状态寄存器
  - 第 1544 页：34.3.5 ADC_DMA_AMT 寄存器，34.3.7 ADC_DMA_RXAx 寄存器
  - 第 1546-1547 页：34.3.11 数据存储格式
